{% block title %}User Details List{% endblock %}

{% block body %}
    <style>
        body {
            background-color: #bf6fed;
            font-size: 18px;
            font-family: Arial, sans-serif;
            margin: 2rem;
        }
        table {
            width: 100%;
            max-width: 700px;
            border-collapse: collapse;
            margin: 0 auto;
            background: white;
        }
        th, td {
            border: 1px solid #333;
            padding: 10px 15px;
            text-align: left;
        }
        th {
            background-color: #a77bd4;
            color: white;
        }
    </style>

    <h1 style="text-align:center; color: white;">User Details</h1>

    <table>
        <thead>
        <tr>
            <th>Name</th>
            <th>Age</th>
            <th>Email</th>
            <th>Role</th>
        </tr>
        </thead>
        <tbody>
        {% for detail in details %}
            <tr>
                <td>{{ detail.name }}</td>
                <td>{{ detail.age }}</td>
                <td>{{ detail.email }}</td>
                <td>{{ detail.role }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">No user details found.</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
